# P2 阶段 commit 合并方案（2026-09-04）

> 状态：**暂缓（用户 2026-09-04 决定：先推进开发计划，commit 合并后续再议）**。方案已成型，规则与 P1 段一致（见 `p1-squash-20260904.md`），随时可照此执行。

## 目标与范围

把 P2 段的 46 个 commit 按主要功能合并为 **6 个**，不改变任何文件内容。

| 项 | 合并前 | 合并后 |
|---|---|---|
| 总 commit 数 | 54 | **14** |
| P2 段 | 46 | **6** |
| 前 2 个初始化 commit | 2 | 2（不动） |
| P1 段 6 组 | 6 | 6（不动） |

相对最初的 72 个 commit，两段合计压掉 58 个。

## 基线

- P2 合并基线 = `3eaf069`（P1 段末组）
- P2 是历史尾部，后面没有 commit → **无需 rebase 尾部**，重建 6 个 commit 后把 `main` 指过去即可（比 P1 那次简单）

## 分组映射（连续区间）

> 硬约束：分组必须是原时间顺序上的**连续区间**。代价是探针相关工作（`af241b5` 在 G1、`cc1f2c4`/`f07a84f`/`9b46eb9` 在 G2、`5397070` 在 G6）被分散到三组——这是连续性的必然结果。

| 新组 | 主题 | 原 commit（序号） | 改动量 |
|---|---|---|---|
| **G1** | feat(P2): 零授权扫描闭环——`:a11y` 进度桥 + 进度通知 + A11y 探针 + settle 标定 | `af241b5` `27d244d` `bd5dd82` `a9c8786` `193f3d4` `aeaf0ea` `cad31df` `322d54e`（1–8） | 7 files +302/−49 |
| **G2** | feat(P2-b): `:a11y` 进程视图挂载通用 API + 探针/滑动测试迁入面板 + 端到端测试设施 | `cc1f2c4` `f07a84f` `f6bd8eb` `9b46eb9` `b32f577` `05c81f9` `05e4e91`（9–15） | 14 files +698/−267 |
| **G3** | feat(P3): 原语扩展——click/dialog/exit/verify/ocrWithRetry/navigate/foreach/setFilter + flow 选择与 weapon_scan | `89562ba` `f83ada3` `097fe53` `0dde75e` `b7ebc43` `1a98b16` `487521e` `9cc5d24` `45184f8`（16–24） | 8 files +507/−21 |
| **G4** | feat(P3): 多分辨率 profile 路由 + 识别健壮性增强 + 守门测试 + README 全量刷新 | `8aa7ffa` `d023926` `7fbb8fa` `1026ea6` `5a3bb26` `7aa976a` `666816e` `9a4bd4b` `4150399` `79fb7ec`（25–34） | 25 files +1648/−1593 |
| **G5** | fix(P3): 零通知依赖改造（扫描控制迁入悬浮窗）+ weapon_scan 链路修复 | `6957d34` `78e5bd0` `942bf0a` `58b31c8` `1f0bfe2` `5adbef8` `83f5cc9`（35–41） | 12 files +489/−137 |
| **G6** | fix: 悬浮窗 clamp/限高/回缩 UI 迭代 + 探针挂载 Looper 修复 | `a1fb3a7` `99e2639` `1424d81` `7825172` `5397070`（42–46） | 4 files +100/−14 |

## 拟定 commit message

### G1 `feat(P2): 零授权扫描闭环——:a11y 进度桥 + 进度通知 + A11y 探针 + settle 标定`
- A11y Overlay 探针：P2 前置 z 序真机验证（TYPE_ACCESSIBILITY_OVERLAY）
- 自适应 settle 标定：替代固定翻页延时
- 滑动测试参数持久化：调参需多次重启 app 验证，重启不丢
- 扫描进度通知（P1 验收项）+ 零授权扫描闭环（通知 action + `:a11y` 进度桥）
- 扫描健壮性：readCount onZero=reopenAndRetry + anchor 断言失败终止扫描
- refactor：滑动时长常量统一到 `InputAccessibilityService.SWIPE_TOTAL_MS`（三处重复消除）
- 守门测试：onZero 重进重试 + anchor 断言终止两场景（FakeOcr 可编程 numberScript）

### G2 `feat(P2-b): :a11y 进程视图挂载通用 API + 探针/滑动测试迁入面板 + 端到端测试设施`
- P2-b 地基：`:a11y` 进程 A11y Overlay 视图挂载通用 API（attachA11yView）
- 探针改桥模式：View 不能跨进程，`:a11y` 小窗须在 `:a11y` 进程内构建
- 滑动测试内嵌面板（缩球执行防遮挡）+ OCR 小字放大 + anchor 宽容重进
- A11y 探针入口迁入面板滑动测试区（删小窗时误删，桥模式不变）
- GOOD 导出通知分享（FileProvider → 飞书/微信，免 adb 拉取）
- 端到端测试基础设施（脚本 + debug 指令同步修复）
- 补全 adb 调试接口：滑动测试与 A11y 探针走 svc action 直发

### G3 `feat(P3): 原语扩展——click/dialog/exit/verify/ocrWithRetry/navigate/foreach/setFilter + flow 选择与 weapon_scan`
- weapon_scan 支持：GoodWeapon + WeaponDictionary + starStrip/锁徽 vote + panel 分支
- ScriptRunner 支持 flow 选择 + adb SCAN_FLOW 触发扫描
- click 两级（fast+fallback）：OCR anchor 校验详情面板 + 重点重试
- dialog 原语：OCR 检测弹窗 + confirmClick 确认处置
- exit + verify 原语
- ocrWithRetry 原语 + CharacterDictionary
- navigate 原语：角色左菜单切换
- foreach 原语 + plan 变量
- setFilter 原语：套装筛选子链

### G4 `feat(P3): 多分辨率 profile 路由 + 识别健壮性增强 + 守门测试 + README 全量刷新`
- 多分辨率坐标守门 + 宽高比失真检测（1920x1080 实测）
- maxPages 翻页早停（调试翻页准确性）
- fix：mappings.json 进 APK（位置错误）+ stopWhen 哨兵保护 + intent-filter 补全
- 分辨率专属 profile 路由 + 2560x1440 占位 profile
- setKey 二级模糊兜底（单字 Dice >= 0.55）
- 详情切换等待：点击后检测面板件名变化，防读旧件
- enterScreen 失败重进前按 BACK 清游戏每日弹窗
- fix：anchor 数字兜底加中文前缀校验（防跨 tab 误配）
- ifMatch 原语 + DoCoverageTest 守门
- docs：README 全量刷新（17 原语/108 守门/adb 调试链/多 profile/真 assets 路径）

### G5 `fix(P3): 零通知依赖改造（扫描控制迁入悬浮窗）+ weapon_scan 链路修复`
- 投影恢复悬浮窗直启 + 扫描控制全部迁入悬浮窗（零通知依赖）
- anchor 前缀校验改 flow 可配（prefixStrict）——修模拟器 OCR 丢前缀全拒
- docs：README 同步 fix53（零通知依赖/悬浮窗控制区/投影直启路径）
- chore：移除 POST_NOTIFICATIONS 权限声明与请求代码（零通知权限）
- CHAIN_ANCHOR_PATHS 补 weapon_tab 映射（weapon_scan 武器 tab 点击曾静默跳过）
- parsePanel 分发放行 weapon_backpack（weapon 分支成死代码的顺序错误）
- WeaponDictionary 二级模糊（单字 Dice，与圣遗物同阈值共享实现）

### G6 `fix: 悬浮窗 clamp/限高/回缩 UI 迭代 + 探针挂载 Looper 修复`
- 悬浮窗 clamp 避状态栏 + 面板限高可滚（竖屏球藏状态栏/横屏面板被裁）
- 扫描启动自动收面板 + clamp 补导航栏避让
- 日志窗口 clamp 补导航栏避让 + body 限高（日志行多时底部超屏被裁，内部 ScrollView 滚动）
- 收起后窗口不回缩致整列吞触摸（fix54 ScrollView 回归：setExpanded 只 GONE 内层 panel，外层 ScrollView 始终 VISIBLE 保有固定高）
- 探针挂载崩溃：binder 线程无 Looper 致 ViewRootImpl 创建失败，改 `Handler(Looper.getMainLooper()).post` + CountDownLatch

## 执行与校验（确认后）

方法同 P1：`reset --hard` 取组末 commit 的树 → `reset --soft` 退回上一新 commit → 带 `GIT_AUTHOR_*`（取自组末 commit，作者 `esc`，日期 2026-09-02/03/04）提交；最后把 `main` 指向新顶端。

备份分支：`backup/p2-presquash-20260904`（指向当前 HEAD `5397070`）。

校验四项：
1. `git diff backup/p2-presquash-20260904 main` 为 0 行
2. `tree(新 G6)` == `tree(5397070)`
3. commit 数 == 14
4. 单元测试 109 例全绿

回滚：`git reset --hard backup/p2-presquash-20260904`
