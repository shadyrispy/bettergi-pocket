# P1 阶段 commit 合并记录（2026-09-04）

## 目标与范围

把 P1 阶段的 24 个细碎 commit 按主要功能合并为 **6 个**语义完整的 commit，不改变任何文件内容。

| 项 | 合并前 | 合并后 |
|---|---|---|
| 总 commit 数 | 72 | **54** |
| P1 段（第 3–26 个） | 24 | **6** |
| 前 2 个（`be49ad5`/`f986d63`） | 2 | 2（**不动**） |
| P2 段（原第 27–72 个） | 46 | 46（原样平移） |

## 为什么保留前 2 个 commit 不动

`origin/main` 只指向 `f986d63`（第 2 个 commit），本地 70 个 commit 全未推送。保留前 2 个 commit 后，`f986d63` 仍是新历史的祖先 —— **push 为普通 fast-forward，不需要 force push**。

## 分组映射（连续区间）

> 硬约束：分组必须是原时间顺序上的**连续区间**。若把靠后的 fix 并进靠前的组，后续组会先"回退"再"重做"同一批改动，中间历史自相矛盾。因此个别 chore 小提交会随相邻功能组归并。

| 新 commit | 主题 | 原 commit（序号） | 改动量 |
|---|---|---|---|
| `2b74487` | feat(P0): 构建环境 pin + FrameSource 帧源抽象 + Swipe/LongPress 动作原语 | `10cb9cb` `8d5d5e7` `09445d1` `157e1b7`（3–6） | 14 files +358/−17 |
| `66c97cc` | feat(P1): 圣遗物扫描主链路——JSON 解释器 + ML Kit OCR + 悬浮窗开关与进度 | `3a2a023` `fc58356` `2e34add` `2319a54` `1b5d8e2`（7–11） | 16 files +2808/−1 |
| `532efd4` | fix(P1): assets/dsl 资产恢复与守门 + 审计修复 + rect-object 下钻兼容 | `b860bb7` `d9d4092` `a544fb7` `abec813`（12–15） | 8 files +1724/−35 |
| `2bab79e` | fix(P1): 多 flow 接入与判据正确性——extractValue / VoteJudges / 止扫语义 | `a436c04` `b7a09bb` `7fddb9d` `62695b3` `9ab1011` `07a8274`（16–21） | 16 files +803/−47 |
| `da03ac1` | feat(P1): total 核对 endConditions + 入库去重 + GOOD 导出纯函数化 | `887b660` `ae61c3b`（22–23） | 4 files +138/−7 |
| `3eaf069` | fix(P1): 三段滑动改逐段 dispatch + callback 链，附滑动测试小窗与 README 重写 | `cb90d4b` `21597da` `58ca324`（24–26） | 5 files +443/−64 |

每个 commit 的 body 保留组内原 commit 的关键根因（如 `Lv.90 被 .90 截胡`、`Mat.get 单像素重载被当整块用`），并以 `Squashed-from:` 尾部记录被合并的原 SHA，保证可追溯。

## 执行方法（soft 模式）

逐组执行「`reset --hard` 取该组末 commit 的树 → `reset --soft` 退回上一新 commit → 提交」，作者与作者日期取自该组最后一个原 commit（`esc` / 2026-09-02）：

```bash
build() {
  local last="$1" msg="$2" prev="$3"
  git reset --hard "$last"      # HEAD+工作树 = 该组末 commit 的树
  git reset --soft "$prev"      # HEAD 退回上一新 commit，树不变
  GIT_AUTHOR_NAME="$(git log -1 --format=%an "$last")" \
  GIT_AUTHOR_EMAIL="$(git log -1 --format=%ae "$last")" \
  GIT_AUTHOR_DATE="$(git log -1 --format=%aI "$last")" \
    git commit -F "$msg"
}
```

随后把 P2 段平移到新顶端：

```bash
git rebase --onto "$N6" 58ca324 main
```

因 `tree(N6) == tree(58ca324)`，46 个 commit 平移**零冲突**。

## 校验（全部通过）

| 校验项 | 结果 |
|---|---|
| `tree(新 P1 段末)` vs `tree(58ca324)` | `f520cbee…` 完全一致 |
| `git diff backup/p1-presquash-20260904 main` | **0 行**（内容零变化） |
| `git rev-parse main^{tree}` | `561f0a1b…` 与重写前一致 |
| commit 数 | 54（预期值） |
| 作者/日期 | `esc` / `2026-09-02` 全部保留 |
| `origin/main`(`f986d63`) 是否祖先 | 是 → push 为 fast-forward |
| 单元测试 | 109 tests, 0 failures |

## 回滚

```bash
git reset --hard backup/p1-presquash-20260904
```

备份分支 `backup/p1-presquash-20260904`（原 HEAD `ba17852`）**保留未删**，确认无误后可自行删除：

```bash
git branch -D backup/p1-presquash-20260904
```

未跟踪文件 `overview.md`、`probe-test-screenshots/` 不受 `reset --hard` 影响，已确认完好。
